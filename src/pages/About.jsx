export default function About(){
    return(<>
         <h1 className="text-5xl text-center my-9 font-bold">About</h1>
         {/* <ul>
            {genres && genres.map((genre)=>(<li key={genre.id}>
                {genre.name}
            </li>))}
         </ul> */}
         </>
    )
}